<!-- Top navbar -->
<nav class="navbar navbar-expand-md navbar-dark bg-primary shadow-lg fixed-top py-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Todo App</a>
        <button
            class="navbar-toggler border-0 shadow-none"
            type="button"
            data-bs-toggle="collapse"
            data-bs-target="#top-navbar"
            aria-expanded="false"
            aria-label="Toggle navigation"
        >
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="top-navbar">
            <ul class="navbar-nav me-auto">
                {% if(user.isLoggedIn) { %}
                <li class="nav-item">
                    <a class="nav-link" href="/todo/user">Todo</a>
                </li>
                {% } %}
            </ul>
            <div class="pt-2 pt-md-0">
                {% if(!user.isLoggedIn) { %}
                <li class="nav-item">
                    <a class="btn btn-warning me-1" href="/auth/signup">Sign up</a>
                    <a class="btn btn-success me-1" href="/auth/login">Login</a>
                </li>
                {% } %} {% if(user.isLoggedIn) { %}
                <a class="btn btn-danger me-1" href="#logoutModal" data-bs-toggle="modal"
                    >Logout</a
                >
                {% } %}
            </div>
        </div>
    </div>
</nav>
{% if(user.isLoggedIn) { %} {%- include('./logout.ejs') %} {% } %}
<!-- End top navbar -->
